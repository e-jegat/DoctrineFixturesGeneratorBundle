services:

    Webonaute\DoctrineFixturesGeneratorBundle\Command\GenerateDoctrineFixtureCommand:
        tags:
            - { name: 'console.command', command: 'doctrine:generate:fixture' }
        arguments:
            - $filesystem: '@filesystem'
            - $registry: '@doctrine.orm.default_entity_manager'
